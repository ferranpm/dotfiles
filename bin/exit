#!/usr/bin/env bash

echo ' s) Shutdown'
echo ' r) Reboot'
echo ' l) Logout'
echo ' c) Cancel'

while [[ $op != @('s'|'r'|'l'|'b'|'c') ]]; do
    read -sn 1 op
done

case $op in
    's') systemctl poweroff ;;
    'r') systemctl reboot ;;
    'l') killall startkde ;;
    'c') echo 'Bye' ;;
esac
