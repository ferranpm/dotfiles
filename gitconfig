[user]
    name = Ferran Pelayo Monfort
    email = ferran.pel.mon@gmail.com

[core]
    editor = vim
    pager = less -R
    autocrlf = input
    excludesfile = ~/.cvsignore

[color]
    diff = auto
    status = auto
    branch = auto
    ui = true

[merge]
    tool = vimdiff

[push]
    default = current

[web]
    browser = firefox

[alias]
    b = branch
    c = commit
    co = checkout
    d = diff
    dt = difftool
    f = fetch
    l = log --pretty=format:'%C(yellow)%h %Cred%ad %C(yellow)%an%Cgreen%d %Creset%s' --date=short --graph
    lo = log --pretty=format:'%C(yellow)%h %Cred%ad %C(yellow)%an%Cgreen%d %Creset%s' --date=short --graph --all
    m = merge
    p = push
    s = status
    v = describe --abbrev=0
    a = add
    ap = add -p
    dc = diff --cached
    r = rm

[sendemail]
    smtpencryption = tls
    smtpserver = smtp.gmail.com
    smtpuser = ferran.pel.mon@gmail.com
    smtpserverport = 587
